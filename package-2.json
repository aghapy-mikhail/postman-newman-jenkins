
{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "Project for storing Postman tests",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "api-tests-production": "newman run tests/fail.json --reporters cli,junit --reporter-junit-export report-2.xml"
  },
  "author": "aghapy-gad",
  "dependencies": {
    "newman": "^3.5.2"
  }
}
